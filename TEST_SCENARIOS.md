# Functional scenarios - not executed

1. New colony, Core and this mod with required providers: open the recreation menu and
   build/use every supplied object; expect correct textures, jobs, joy and no log errors.
2. Repeat in English and French; expect readable labels/reports and no unresolved keys.
3. Enable all extracted packages with the reduced Joy Preservation; expect one owner per
   definition and no missing references. Test optional providers present and absent.
4. On a copy of a pre-split save, enable the new package before loading; inspect existing
   buildings/items, active jobs and recreation tolerance, then save/reload. Expected:
   preserved references without new errors. This expectation is not a claimed result.
5. Native power, flick and refuelling controls where applicable: test each state and reload.
   No custom settings page or MainButton should appear.