<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Step definitions for the two pre-split features in ../Mod/Pickle/Features. Only Pickle loads this assembly:
    it lives in a test companion, never in the Mod/ folder that goes to the Workshop.
    Build: dotnet build Tests/Pickle/PreSplit/Source/EponaInstrumentsRenew.PreSplitSteps.csproj -c Release
  -->
  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <AssemblyName>EponaInstrumentsRenew.PreSplitSteps</AssemblyName>
    <RootNamespace>EponaInstrumentsRenew.PickleSteps.PreSplit</RootNamespace>
    <LangVersion>latest</LangVersion>
    <Nullable>disable</Nullable>
    <Deterministic>true</Deterministic>
    <DebugType>none</DebugType>
    <OutputPath>..\Mod\Pickle\Assemblies\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- Compile-time stubs only: the game, Harmony and the Pickle mod supply the real assemblies. -->
    <PackageReference Include="Krafs.Rimworld.Ref" Version="1.6.*-*" ExcludeAssets="runtime" PrivateAssets="all" />
    <PackageReference Include="Lib.Harmony" Version="2.*" ExcludeAssets="runtime" PrivateAssets="all" />
    <PackageReference Include="RimWorks.Pickle.Ref" Version="4.*" ExcludeAssets="runtime" PrivateAssets="all" />
  </ItemGroup>

</Project>
